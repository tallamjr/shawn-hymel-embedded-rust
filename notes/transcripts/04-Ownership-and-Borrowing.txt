Rust's ownership model can be quite daunting for newcomers, and that's okay.
It helps solve a number of major memory vulnerabilities in older languages without relying on the overhead of a garbage collection system.
It allows for fast and memory-safe programs, but it can be a little confusing at first.
In the previous two episodes, we wrote a couple of basic embedded Rust applications to blink an LED and send strings out over the USB serial port.
We're going to take a step back now to dive into the basic rules behind the ownership model.
We won't need any hardware for this episode, as it will just be quick snippets of code that can run on your computer.
Before we start, we should talk about the different places in memory where your data can be stored.
In many applications, you will find RAM for your program mapped something like this.
We'll ignore the program instructions or .text data for now, as we'll assume that's in a separate section of flash memory.
At the lower RAM addresses, you'll find a place for initialized static and global variables, often abbreviated as .data.
Then, you'll find an area for uninitialized static and global variables, which is usually labeled .bss.
In many embedded systems, you'll find an area for data that persists between reboots.
This usually gets a label like .uninit or .persistent.
After that, you'll find the heap.
This is a general pool of memory for dynamic allocation.
If you've worked in C, when you call something like malloc, it will reserve a section of heap for you to use in your program.
Usually, the compiler does not know how much memory needs to be allocated at compile time,
so it just lets the heap dynamically grow and shrink as needed as the program runs.
In most cases, including our Raspberry Pi Pico 2, the heap will grow towards higher RAM addresses.
In the last section of RAM, toward the highest addresses, you'll find the stack.
This is a last-in, first-out data structure for allocating data for local variables, function parameters, and return addresses.
Data here usually gets automatically cleaned up as variables go out of scope.
While the maximum stack size can be determined at program startup,
the used portion will grow towards lower RAM addresses as needed.
If heap and stack meet, bad things happen.
This is known as virtual address space exhaustion, or simply an address space collision in colloquial terms.
You might simply not be able to allocate memory, or data might get corrupted, or your program might crash.
Some embedded hardware or software frameworks add protections and guards to help prevent such collisions.
We won't get into them in this series, as nothing we're doing uses that much memory.
Another way around this is to avoid using the heap altogether,
which is a common practice in embedded systems.
Because you can determine the maximum stack size at initialization,
you can easily test to ensure that your program won't run out of memory.
I bring this up because most of the ownership examples in the Rust book rely on heap examples.
While you can enable the heap in many embedded systems,
we'll actually be creating mostly demos without the heap and without the standard library,
which gives us types like strings and vectors.
Let's take a look at the three main ownership rules in Rust.
If you worked through chapter four of the Rust book,
you'll probably notice that they used strings for all of their examples,
which is great, except for the problem that most of the time these string types exist in heap,
and string is part of the standard library, which we won't be using for most of our embedded examples.
So let's find a different way to do this that still demonstrates all of these ownership examples.
In the Docker container, I'm going to go into workspace apps.
I'm going to create a new project with cargo new.
Let's call it ownership examples.
We'll say this is a binary or executable,
and we'll say version control system is set to none.
With that created, let's open up our ownership examples,
go into source, open up main.
Let's delete this main function so we can start creating our own.
Instead of strings, we're going to use this custom sensor reading struct.
It's just some dummy values put in a struct.
We're going to have one value be named value,
and that's going to be of type unsigned 16-bit integer,
and another that is a timestamp MS for milliseconds,
and that is an unsigned 32-bit integer.
This might be a wrapper for some sort of sensor reading you might get.
By using primitives inside of a struct,
we can guarantee that this information is stored in the stack,
but we can still use references to it in order to demonstrate the ownership and borrowing rules.
The first rule, and this is right out of the Rust book,
says that each value in Rust has an owner.
In this case, we'll let reading, our variable,
be the owner of this new sensor reading struct with this data that's been filled out.
If you haven't played with it yet,
note that this uses the values or value and timestamps display trait if that's available.
So if that's been implemented, which it should be for most primitives in Rust,
we can display them using these curly braces.
Let's save this.
We'll go into our ownership examples.
We'll say cargo run.
That should build it.
And sure enough, that prints it out.
So this demonstrates that reading has ownership.
Okay, that's not very useful.
So let's see how the rules expand to flesh out the whole ownership model.
The second rule states that there can only be one owner at a time.
So let's create our immutable reading variable,
and we'll set it to be the owner of our sensor reading,
which has the data value two and timestamp of 100.
Now let's create a new variable.
We'll call this one new owner,
and we're going to say equals reading.
In Rust, this is considered a direct transfer of ownership,
or it's also called a move.
Now, at this point, so at the end of this line,
new owner is the owner of that sensor reading we created.
If you try to do this to a primitive,
note that Rust automatically performs a copy of this primitive.
So reading would be value three,
and new owner would also be value three,
because that value was copied to new owner, not transferred.
It assumes you want to copy things like primitives
because there's not a lot of data in them.
Let's go back to this struct,
and then by default,
this is a move of ownership because it's a struct.
You'll see this for things like strings and vectors in the standard library as well.
This is an actual move.
So what happens if we try to print the values inside of the sensor reading struct
after we've moved that struct to a different owner?
Save your file.
Say cargo run,
and sure enough,
you get a compile time error.
This says borrow of moved value reading.
So the value moved here on this line that we showed earlier,
and you can no longer use it because essentially this doesn't work anymore.
This is no longer the owner of sensor reading.
If you were to implement clone,
the clone function for this struct,
and give it a specification on how to actually copy the values within,
this might actually work,
and you could call clone here.
But since we did not do that,
this fails.
I'm going to make a note about where that's failing.
And then as can be expected,
we can use our new owner and print out the values within.
So let's save this.
Let's run it again.
Sure enough,
it builds and prints out the values without a problem.
So I mentioned that you could implement clone for this.
You can also do deep copies of things if it's been implemented,
and arrays actually by default implement the copy trait.
Assuming each of the elements also implement the copy trait.
Let's see an example of that.
We'll do a little Fibonacci sequence here,
and then we will do something that looks like our move earlier,
but because arrays by default implement copy,
assuming all the individual elements implement copy,
Rust thinks you're trying to copy the array here.
Keep this in mind if you have large arrays,
because this might be a deep copy of the array,
which could consume a lot of clock cycles.
So keep that in mind.
So as expected,
we can actually print out or use the values both in my array and my copy.
This colon question mark inside these curly braces,
this is the debug format specifier.
This uses the debug trait that's implemented by default on things like arrays
in order to print them out.
Let's save this.
Let's save this.
We'll say cargo run.
We get this warning saying we're not using sensor reading.
That's okay.
But otherwise, it prints out both arrays because it has been deep copied in this line here.
All right, let's get rid of this.
The third rule is that when the owner goes out of scope, the value will be dropped.
Let's make a note about what scope actually means here.
Scope is the region of code where a variable is valid and can be accessed or seen.
This is usually between curly braces in things like functions or loops.
But note that we can just put curly braces in the middle of a function right here.
This creates a new scope for us.
So let's see what happens when we try to run this where we initialize this reading variable inside of a new scope,
exit that scope, and then try to print it.
So let's save and we'll try to run it.
Sure enough, we get some errors.
It cannot find reading in this scope.
So let's make a note about this.
According to rule three, reading went out of scope right here.
In fact, the compiler sees that it's no longer used within this scope and drops it right at this point.
That means freeing up the memory consumed by sensor reading.
Instead of a garbage collection system used in other languages like Python and JavaScript that come through
and look for pieces of memory that are no longer referenced,
Rust attempts to do this essentially all in compile time.
There is a little bit of runtime overhead to actually do the free on the back end.
But the compiler sees that reading is no longer used in this scope
and immediately frees up the memory taken up by sensor reading.
So the value is dropped.
The reading variable is no longer valid,
which means we cannot use it here any longer.
This helps prevent things like use after free,
double free,
double pointers,
and memory leaks.
These are common memory errors you'll find in things like C and C++.
So we know this line doesn't work anymore.
And to demonstrate this even further,
let's create a new function.
When we pass in a value like this to a function,
we say that ownership of that value is consumed by the function.
It's actually moved or transferred in as we saw in rule number two.
So we can pass in reading,
but then this function now owns reading.
This will work.
It will print out right here.
But as we saw with the scope earlier,
because this is a new scope within the function,
reading will go out of scope right at this point.
So the value is actually dropped and the memory is freed.
Let's try this.
We will pass in reading to this print reading function.
Ownership is passed to the function and this should print out.
Let's save it.
Let's go ahead and run.
Great.
It worked.
We printed out three and a hundred from within this function.
So what do you think will happen right here?
As I mentioned,
we passed in reading
and we passed ownership to this function.
Reading goes out of scope
and that's it.
We no longer have reading here.
So this should fail.
We should not be able to use reading any longer because it went out of scope.
Let's save this.
We will run it.
Sure enough.
Here is that error.
This might be a little cryptic at first where it says borrow of moved value reading.
That's because print LN is actually using some references,
which we'll explore next.
So we're trying to borrow the values here,
but it's already been moved.
And in fact, scope dropped here and the value dropped.
So that's the error you will see if you try to use something
that's already been moved and consumed
and you no longer have ownership of it.
So we have this error right now.
Okay.
How do we fix this?
One way to do this
is we can create a new function.
So I'm going to comment that out
and under here,
let's recreate this function.
Great.
Same signature.
So far we have the same function name.
We are passing in reading.
It is of type sensor reading.
But guess what?
What if we went ahead and returned ownership of that exact value?
So that is giving the return type of sensor reading.
We have the exact same internals of the function previously,
but we're going to actually return that value.
Note that this is the verbose way to say return a value from a function.
We're going to say return and the name of that variable.
However,
you'll find that many rust programmers or rustations won't use this verbose syntax.
You'll just see something like this,
which means return reading.
So we don't use the return keyword and we don't use a semi colon here.
And this is just shorthand for saying return this value.
You'll see this a lot in rust.
So if you don't see a semi colon,
it means that the value is being returned up to the color.
Okay.
So the last thing we need to do is we called our function.
Let's save this,
but where we called the function,
we expect it to return reading from it.
So we gave ownership to the function,
the function returned ownership and we put that ownership back in the same
variable.
Now we should be able to use it again.
So we should see the values printed out twice.
We saved that.
Let's say cargo run and you notice something interesting here where it says,
cannot assign twice to the immutable variable reading.
Aha,
because we initially by default set this to immutable and we're trying to
overwrite that value.
Even if it's the same value to fix this,
we just say,
let this variable be mutable.
And in fact,
many of the compiler errors will give you some really good hints here to say,
Hey,
why don't you add mutable or the mute keyword when you define the variable?
Great.
Let's do that.
We'll save and we'll run again.
There we go.
It's printed twice.
So we've successfully demonstrated how we can pass ownership around and noted
that values are dropped and memory is freed up.
Once the owner goes out of scope,
raw pointers in rust like pointers and see store the memory address of another
value references.
On the other hand are like a safety wrapper around raw pointers.
They come with additional compile time checks to prevent most memory issues like use after free and double freeze that can crash programs or introduce vulnerabilities in applications written in other languages.
Like CMC plus plus they do not have any additional runtime overhead and operate just like pointers under the hood.
The rust compiler,







The rust compiler,



The rust compiler,
The rust compiler ensures that you do not use references in an unsafe manner.
In the vast majority of cases,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
!
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
!
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
!
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
!
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,

The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
The rust compiler,
